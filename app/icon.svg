<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" role="img" aria-label="DevTools">
  <!-- Matches the in-app wordmark: the same blue tile and terminal glyph, so
       the browser tab and the sidebar read as the same product. -->
  <rect width="32" height="32" rx="7" fill="#236DC9"/>
  <g fill="none" stroke="#FFFFFF" stroke-width="3.2" stroke-linecap="round" stroke-linejoin="round">
    <!-- Deliberately chunky: this has to survive being drawn at 16px. -->
    <path d="M9 11.5 L14.5 16 L9 20.5"/>
    <path d="M17.5 21 L23 21"/>
  </g>
</svg>
